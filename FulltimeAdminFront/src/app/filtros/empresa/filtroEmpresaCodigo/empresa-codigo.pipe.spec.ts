import { EmpresaCodigoPipe } from './empresa-codigo.pipe';

describe('EmpresaCodigoPipe', () => {
  it('create an instance', () => {
    const pipe = new EmpresaCodigoPipe();
    expect(pipe).toBeTruthy();
  });
});
