import { EmpresaDescripcionPipe } from './empresa-descripcion.pipe';

describe('EmpresaDescripcionPipe', () => {
  it('create an instance', () => {
    const pipe = new EmpresaDescripcionPipe();
    expect(pipe).toBeTruthy();
  });
});
