import { EmpresaIdPipe } from './empresa-id.pipe';

describe('EmplCodigoPipe', () => {
  it('create an instance', () => {
    const pipe = new EmpresaIdPipe();
    expect(pipe).toBeTruthy();
  });
});
